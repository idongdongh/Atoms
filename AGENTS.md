# Atoms 项目协作规则

## 项目目标

本项目用于构建一个面向 Web 的 AI 应用生成平台。产品体验参考 Dyad，但系统架构采用远程 Control Plane、Workspace Plane 和 Execution Plane，不照搬 Electron 实现。

当前项目处于规划阶段。开始实现前先阅读：

- `README.md`
- `docs/README.md`
- `docs/phase-1-development-plan.md`

## 工作方式

- 模糊需求先跟用户确认，不要直接实现。
- 先明确用户目标、系统边界和可验证的完成标准，再编写代码。
- 优先选择最简单、直接且可持续演进的实现，不提前添加未被当前阶段需要的抽象。
- 修改必须限定在当前任务范围内，不顺手重构无关代码。
- 密钥、Token、密码和真实用户数据不得写入代码、日志、测试夹具或提交历史。
- Node.js 项目统一使用 `pnpm`，不得引入 npm 或 Yarn 锁文件。
- 所有实现必须通过与风险相匹配的类型检查、单元测试、集成测试或端到端测试。

## 架构硬约束

- Control Plane 不得直接执行用户生成代码。
- 用户代码必须在隔离 Sandbox 中运行，并设置 CPU、内存、磁盘、进程、网络和生命周期限制。
- Workspace 是项目文件和 Git 操作的唯一入口，其他服务不得绕过 Workspace 直接修改项目文件。
- Git 是代码版本的事实来源；数据库只保存项目、消息、运行状态和版本索引等元数据。
- Agent 使用结构化 Tool Calling 和 Schema 校验，不以自由文本或自定义 XML 作为长期执行协议。
- 每个项目同一时刻最多允许一个写入型 Agent Run；所有写操作必须具备幂等键和审计记录。
- Preview 必须与主应用分域，并通过短期签名授权访问。
- API、Agent Worker、Workspace 和 Sandbox 之间通过显式 Contract 通信，不共享内部实现。
- 共享 Contract 使用运行时 Schema 校验，不能只依赖 TypeScript 静态类型。
- 长任务事件必须持久化、可重放，并支持客户端断线恢复。

## 文档约定

- 根 `README.md` 面向开发者和评估者，说明项目定位、启动方式和完整目录结构。
- `docs/README.md` 说明文档目录的职责、命名与维护方式。
- 架构决策在实现前记录；开发计划发生变化时，先更新文档再调整实现。
- 临时调研内容不得长期散落在项目根目录，确认结论后应归入 `docs/` 或删除。

## 上下文持续性

当上下文窗口接近上限时，它将自动压缩，让工作能够从上次中断的位置继续。不要因担心令牌预算而提前停止任务；在上下文刷新前保存当前进度和状态，保持最大限度的自主性和持久性，直到目标完成或遇到必须由用户决定的真实阻塞。
